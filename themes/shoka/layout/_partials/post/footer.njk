<div class="meta">
  {%- if date(post.date) != date(post.updated) or time(post.date) != time(post.updated) %}
  <span class="item">
    <span class="icon">
      <i class="ic i-calendar-check"></i>
    </span>
    <span class="text">{{ __('post.edited') }}</span>
    <time title="{{ __('post.modified') + __('symbol.colon') + full_date(post.updated) }}" itemprop="dateModified" datetime="{{ moment(post.updated).format() }}">{{ date(post.updated) }}</time>
  </span>
  {%- endif %}

  {%- if theme.waline.serverURL and theme.waline.visitor %}
  {#- Waline's counter finds its target by selector and reads data-path, rather
      than MiniValine's convention of putting the path in the element id. #}
  <span class="item" title="{{ __('post.views') }}">
      <span class="icon">
        <i class="ic i-eye"></i>
      </span>
      <span class="text">{{ __('post.views') }}</span>
      <span class="waline-pageview-count" data-path="/{{ page.path | replace('index.html', '') }}"></span>
      <span class="text">{{ __('symbols_count_time.view') }}</span>
  </span>
  {%- endif %}
</div>
